# daigo 爬取監控與失敗通知 — 實作規格

目標：知道 generic 通用抓取器（佔營收約 40%、涵蓋 260 個網域）到底有多少抓不到、
為什麼抓不到，並在真的壞掉時馬上知道。

**現況問題：失敗的爬取不留下任何痕跡。** 客人貼連結、抓不到、關掉走人 —— Shopify 上
沒有商品也沒有訂單。目前手上所有資料都是成功案例，分母不存在。Zeabur 的 log 每次
重新部署就清空，撈不到歷史。

---

## 一、記錄什麼

**成功和失敗都要記。** 只記失敗算不出失敗率，而失敗率才是能拿來排序的東西。
成功紀錄另外還給出：各網域實際被貼的次數（≠ 建成商品數）、爬取耗時。

每次 `/api/scrape`（以及 `/api/create-order` 走爬取的路徑）結束時記一筆：

| 欄位 | 說明 |
|---|---|
| `ts` | UTC 時間戳 |
| `domain` | 正規化後的網域（去 `www.`、小寫） |
| `platform_id` | 走到哪支 Platform（`generic` / `rakuten` / `yahoo_store` …） |
| `source` | Platform 底下實際命中的 Source（httpx / official_api / selenium / playwright） |
| `ok` | 成功或失敗 |
| `failure_kind` | 失敗分類，見下節；成功時留空 |
| `http_status` | 有的話 |
| `elapsed_ms` | 爬取耗時 |
| `error_brief` | 例外類型 + 訊息前 200 字，**不要存完整 traceback** |
| `url_path` | 路徑部分即可，**不要存完整 query string**（可能含個資或 session） |

**不要記客人身分、IP、session。** 這份資料的用途是判斷「哪個網域該修」，
不需要知道是誰貼的。

---

## 二、失敗分類（關鍵：在寫入時就分好，不要事後看字串猜）

五類的處置方式完全不同。分類錯了，這整套就只是一張看不懂的表。

| `failure_kind` | 判斷依據 | 要不要修 |
|---|---|---|
| `blocked` | HTTP 403 / 429；回應內容含 Akamai / Cloudflare / reCAPTCHA 特徵；已知的擋機房 IP 行為 | 要 —— 需住宅代理 |
| `parse_failed` | HTTP 200 拿到頁面，但抽不出必要欄位（標題或價格） | 要 —— 需寫解析 |
| `not_found` | HTTP 404 / 410；或頁面明確顯示商品已下架、販売終了 | **不用修** |
| `timeout` | 連線或讀取超時、driver 逾時 | 觀察 —— 可能只是慢 |
| `other` | 以上都不是 | 看情況 |

**分類會有誤判，這是預期內的。** 404 有可能是爬蟲組錯網址而不是真下架；
timeout 有可能是軟性阻擋。所以摘要裡 `not_found` 那組仍然給數字 ——
**某天 404 突然暴增，那本身就是訊號。**

客人貼錯連結（圖片直連、搜尋結果、短網址）已經被 `scrapers/base.py` 的
`detect_invalid_link()` 擋在爬取之前，**不進這份紀錄**。

---

## 三、存哪裡

**不開新的資料庫服務。** 使用者不想多一個地方管理。

容器內先寫本機檔案（append-only JSONL），每天寄出摘要後可清空當日檔。
Zeabur 重啟會清空，所以：

- **摘要信本身就是持久化**。Gmail 是可搜尋的儲存，不用另外管理
- 若容器在寄信前重啟，那段資料就丟了 —— **可接受**，這是趨勢資料不是帳務資料
- 不要為了「不能掉資料」去接資料庫，那違背了不多管一個服務的前提

寫入要 **fail-safe**：記錄失敗絕不能影響爬取本身。整段包 try/except，
出錯就 print 一行然後繼續，不要 raise。

---

## 四、通知

收件人：`omishoninjp@gmail.com`

### 每日摘要（固定時間一封）

依錯誤類型分組，**只詳列要決定的**：

```
【daigo 每日爬取摘要】2026-08-28

成功 47 / 失敗 12（成功率 79.7%）

■ 需要處理
  解析失敗  6 次
    store.plusmember.jp    4 次  ← 連續 3 天失敗
    fujitaka-japan.com     2 次

  被擋（機房 IP）  3 次
    zozo.jp  3 次  ← 已知問題

■ 不用管
  已下架/404  2 次
  超時  1 次

■ 今日最慢
  suruga-ya.jp  平均 18.3s（3 次）
```

- 「不用管」那組**只給數字不列細節**，不佔注意力
- 「連續 N 天失敗」的標記要有 —— 那是判斷「改版壞掉」vs「偶發」的關鍵
- 當天零失敗就寄一行「今日無失敗」，**不要不寄** ——
  沒收到信要能區分「沒問題」和「系統掛了」

### 即時警報（只在真的緊急時）

三個條件，任一觸發就立刻寄：

1. 同一網域**連續失敗 5 次**（通常代表改版壞了，不是偶發）
2. 當日整體成功率**掉到 50% 以下**（且當日樣本 ≥ 10 次，避免小樣本誤報）
3. **建單路徑出錯** —— `/api/create-order` 拋出未預期例外。
   那是直接掉單，不是爬取品質問題

同一個警報**一天內不重複寄**（同網域同原因），否則一次改版會轟炸整個信箱。

---

## 五、寄信方式

用 **Resend** 或 **SendGrid** 免費層，API key 進 Zeabur 環境變數。

**不要用 Gmail SMTP 直寄** —— 要應用程式密碼、容易被判垃圾信，又多一組憑證要管。

---

## 六、上線步驟（重要）

1. **先只記錄，不寄信，跑兩天。**
   看實際的量和分類準不準，避免一上線就被自己的信淹沒。
2. 兩天後把 JSONL 匯出來看一次，確認：
   - 失敗分類有沒有明顯誤判（尤其 `not_found` 和 `timeout` 的界線）
   - 每日失敗筆數量級 —— 決定摘要要不要再收斂
3. 確認沒問題再開每日摘要，最後才開即時警報。

---

## 七、驗證

- **不要只跑 `py_compile`。** 這個專案 2026-08 的 `gql_nodes` NameError 就是
  py_compile 抓不到、只在特定分支才爆的錯
- 用**真實會失敗的網址**測每一種 `failure_kind`，確認分類正確：
  找一個 404 的、一個會被擋的、一個結構解析不出來的
- 測記錄層本身壞掉時**不影響爬取** —— 故意讓寫入拋例外，確認爬取照常回傳
- 摘要信先寄給自己看一次格式，再正式接上

---

## 這份規格要達成的事

一個月後使用者打開信箱，應該能在三分鐘內回答：
**「這個月哪個網域最該修？修了能救回多少？」**

如果做出來的東西回答不了這個問題，就是做錯了。
